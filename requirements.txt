# Dependencies are declared in pyproject.toml (single source of truth).
# This file installs the PyTorch framework plus the test/lint tooling — enough
# to run `pytest` and every entrypoint under edge_ai_compression/ and src/.
#
# For the original TensorFlow/Keras scripts (train_baseline.py, benchmark.py,
# quantize_model.py, distill_model.py) also install the `tf` extra:
#     pip install -e ".[tf]"       # TensorFlow + Keras
#     pip install -e ".[tf-mot]"   # adds the pruning toolkit for prune_model.py
# For plotting: pip install -e ".[viz]"
# For everything at once: pip install -e ".[all]"
-e .[dev]
