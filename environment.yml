name: edge-ai-compression
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # Full environment: PyTorch framework + TensorFlow scripts + viz + dev.
      # Dependency versions are declared in pyproject.toml.
      - -e .[all]
